# include <stdio.h>
int main(){
        int a, b, sum;

        printf("Enter a value of a: ");
        scanf("%d", &a);

        printf("Enter a value of b: ");
        scanf("%d", &b);
 
        sum = a + b;

        printf("The sum of a and b is %d \n.",sum);
}
